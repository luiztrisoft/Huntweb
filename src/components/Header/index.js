import React from 'react';

import "./styles.css";

const Header = () => <header id="main-header">JSHunt</header>;

export default Header;

/**
 * 1. Stateless components: Criando componentes dentro do react com apenas funções. 
 * 2. É a mesma coisa que criar uma classe extendendo componente porém bem menos verbosa para criação
 *       de componentes mais simples.
 * 3. Note que nem precisamos de () depois de => no retorno pois tudo foi definido em uma única linha
 * 
 */